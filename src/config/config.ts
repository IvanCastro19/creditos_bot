import {
    SERVER,
    MONGO,
    API_360,
    CUSTOMER_SERVICE
} from './constants/index';

const config = {
    server: SERVER,
    mongo: MONGO,
    dialog: API_360,
    customer_service: CUSTOMER_SERVICE
};

export default config;